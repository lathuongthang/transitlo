(()=>{const box=document.createElement('div');box.className='lightbox';box.innerHTML='<button aria-label="Close">×</button><img alt="">';document.body.appendChild(box);const img=box.querySelector('img');document.querySelectorAll('.gallery figure,.references figure').forEach(el=>el.addEventListener('click',()=>{img.src=el.querySelector('img').src;box.classList.add('open')}));box.addEventListener('click',e=>{if(e.target===box||e.target.tagName==='BUTTON')box.classList.remove('open')});document.addEventListener('keydown',e=>{if(e.key==='Escape')box.classList.remove('open')})})();